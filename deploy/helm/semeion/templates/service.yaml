apiVersion: v1
kind: Service
metadata:
  name: {{ include "semeion.fullname" . }}
  labels:
    {{- include "semeion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "semeion.selectorLabels" . | nindent 4 }}
